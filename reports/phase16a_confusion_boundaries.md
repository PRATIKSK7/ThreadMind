# Phase 16A — Confusion Boundaries

## amazon_locker ↔ delivery_missing (Count: 3)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## digital_kindle ↔ other_support (Count: 3)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## echo_alexa ↔ other_support (Count: 3)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## other_support ↔ promotions_pricing (Count: 3)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## other_support ↔ product_availability (Count: 3)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## account_billing ↔ other_support (Count: 2)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## account_access ↔ delivery_wrong_item (Count: 2)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## grocery_fresh ↔ other_support (Count: 2)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## other_support ↔ returns_refunds (Count: 1)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

## digital_prime_video ↔ other_support (Count: 1)
**OVERLAP**: These intents frequently share vocabulary (e.g. 'account', 'missing'). The customer often states a symptom that matches one intent while the root cause aligns with the other.
**DECISION RULE**: The true problem must be identified by the resolution requested, not the symptom described.

